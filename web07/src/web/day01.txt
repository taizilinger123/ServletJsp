1.状态管理
   (1)什么是状态管理
      将浏览器与web服务器之间的多次交互作为一个整体来处理，并且将多次交互所涉及的数据(即状态)
      保存下来。
   (2)cookie
      1)什么是cookie?
        服务器临时保存在浏览器端的少量的数据。
      2)工作原理
           当浏览器访问服务器时，服务器可以将少量数据以set-cookie消息头的方式发送给浏览器，浏览器
           会将这些数据临时保存下来。
           当浏览器再次访问服务器，会将之前保存的这些数据以cookie消息头的方式发送给服务器。
      3)添加cookie
        Cookie c = new Cookie(String name,String value);
        response.addCookie(c);
        http://localhost:8888/web07/addCookie     
        http://localhost:8888/web07/findCookie
      4)读取cookie
        Cookie[] request.getCookies();
        String cookie.getName();
        String cookie.getValue();
      5)编码问题
        cookie只能保存合法的ascii字符,对于中文，需要将其转换成相应的ascii字符的表示形式。
        import java.net.URLEncoder;String URLEncoder.encode(String str,String charset);
        import java.net.URLDecoder;String URLDecoder.decode(String str,String charset);
        注：添加cookie时，建议最好统一编码处理。
      6)生存时间问题       
        缺省情况下，浏览器会把cookie保存在内存里面，只要浏览器不关闭，cookie就会一直存在,
        浏览器关闭，cookie就会被销毁。
        setMaxAge(int seconds);
        注：单位是秒。365*24*60*60
        seconds>0浏览器会将cookie保存在硬盘上，如果超过了指定的时间，则cookie会被删除。
        seconds<0缺省值
        seconds=0删除cookie 
        比如，要删除一个名称为"cart"的cookie,
        Cookie c = new Cookie("cart","");
        c.setMaxAge(0);
        response.addCookie(c);
      